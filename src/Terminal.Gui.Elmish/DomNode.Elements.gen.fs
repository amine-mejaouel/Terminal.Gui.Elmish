// This file is auto-generated by the Terminal.Gui.Elmish.Generator.
// Do not edit by hand.

namespace Terminal.Gui.Elmish

open Terminal.Gui.App
open Terminal.Gui.ViewBase
open Terminal.Gui.Views


module internal ViewDom =

  let applyProps (domNode: DomNode, props: Props) =
    let view = domNode.View

    // Properties
    props
    |> Props.tryFind PKey.View.Arrangement
    |> Option.iter (fun v -> view.Arrangement <- v)

    props
    |> Props.tryFind PKey.View.AssignHotKeys
    |> Option.iter (fun v -> view.AssignHotKeys <- v)

    props
    |> Props.tryFind PKey.View.BorderStyle
    |> Option.iter (fun v -> view.BorderStyle <- v)

    props
    |> Props.tryFind PKey.View.CanFocus
    |> Option.iter (fun v -> view.CanFocus <- v)

    props
    |> Props.tryFind PKey.View.CommandsToBubbleUp
    |> Option.iter (fun v -> view.CommandsToBubbleUp <- v)

    props
    |> Props.tryFind PKey.View.ContentSizeTracksViewport
    |> Option.iter (fun v -> view.ContentSizeTracksViewport <- v)

    props
    |> Props.tryFind PKey.View.Cursor
    |> Option.iter (fun v -> view.Cursor <- v)

    props |> Props.tryFind PKey.View.Data |> Option.iter (fun v -> view.Data <- v)

    props
    |> Props.tryFind PKey.View.DefaultAcceptView
    |> Option.iter (fun v -> view.DefaultAcceptView <- v)

    props
    |> Props.tryFind PKey.View.Enabled
    |> Option.iter (fun v -> view.Enabled <- v)

    props |> Props.tryFind PKey.View.Frame |> Option.iter (fun v -> view.Frame <- v)

    props
    |> Props.tryFind PKey.View.HasFocus
    |> Option.iter (fun v -> view.HasFocus <- v)

    props
    |> Props.tryFind PKey.View.Height
    |> Option.iter (fun v -> view.Height <- v)

    props
    |> Props.tryFind PKey.View.HotKey
    |> Option.iter (fun v -> view.HotKey <- v)

    props
    |> Props.tryFind PKey.View.HotKeySpecifier
    |> Option.iter (fun v -> view.HotKeySpecifier <- v)

    props |> Props.tryFind PKey.View.Id |> Option.iter (fun v -> view.Id <- v)

    props
    |> Props.tryFind PKey.View.IsInitialized
    |> Option.iter (fun v -> view.IsInitialized <- v)

    props
    |> Props.tryFind PKey.View.MouseHighlightStates
    |> Option.iter (fun v -> view.MouseHighlightStates <- v)

    props
    |> Props.tryFind PKey.View.MouseHoldRepeat
    |> Option.iter (fun v -> view.MouseHoldRepeat <- v)

    props
    |> Props.tryFind PKey.View.MousePositionTracking
    |> Option.iter (fun v -> view.MousePositionTracking <- v)

    props
    |> Props.tryFind PKey.View.PreserveTrailingSpaces
    |> Option.iter (fun v -> view.PreserveTrailingSpaces <- v)

    props
    |> Props.tryFind PKey.View.SchemeName
    |> Option.iter (fun v -> view.SchemeName <- v)

    props
    |> Props.tryFind PKey.View.ShadowStyle
    |> Option.iter (fun v -> view.ShadowStyle <- v)

    props
    |> Props.tryFind PKey.View.SuperViewRendersLineCanvas
    |> Option.iter (fun v -> view.SuperViewRendersLineCanvas <- v)

    props
    |> Props.tryFind PKey.View.TabStop
    |> Option.iter (fun v -> view.TabStop <- v)

    props |> Props.tryFind PKey.View.Text |> Option.iter (fun v -> view.Text <- v)

    props
    |> Props.tryFind PKey.View.TextAlignment
    |> Option.iter (fun v -> view.TextAlignment <- v)

    props
    |> Props.tryFind PKey.View.TextDirection
    |> Option.iter (fun v -> view.TextDirection <- v)

    props |> Props.tryFind PKey.View.Title |> Option.iter (fun v -> view.Title <- v)

    props
    |> Props.tryFind PKey.View.UsedHotKeys
    |> Option.iter (fun v -> view.UsedHotKeys <- v)

    props
    |> Props.tryFind PKey.View.ValidatePosDim
    |> Option.iter (fun v -> view.ValidatePosDim <- v)

    props
    |> Props.tryFind PKey.View.VerticalTextAlignment
    |> Option.iter (fun v -> view.VerticalTextAlignment <- v)

    props
    |> Props.tryFind PKey.View.Viewport
    |> Option.iter (fun v -> view.Viewport <- v)

    props
    |> Props.tryFind PKey.View.ViewportSettings
    |> Option.iter (fun v -> view.ViewportSettings <- v)

    props
    |> Props.tryFind PKey.View.Visible
    |> Option.iter (fun v -> view.Visible <- v)

    props |> Props.tryFind PKey.View.Width |> Option.iter (fun v -> view.Width <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.Accepted, view.Accepted)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.Accepting, view.Accepting)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.Activated, view.Activated)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.Activating, view.Activating)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.AdvancingFocus, view.AdvancingFocus)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.BorderStyleChanged,
      view.BorderStyleChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.CanFocusChanged,
      view.CanFocusChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.ClearedViewport,
      view.ClearedViewport
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.ClearingViewport,
      view.ClearingViewport
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.CommandNotBound,
      view.CommandNotBound
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.ContentSizeChanged,
      view.ContentSizeChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.ContentSizeChanging,
      view.ContentSizeChanging
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.Disposing, view.Disposing)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.DrawComplete, view.DrawComplete)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.DrawingContent, view.DrawingContent)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.DrawingSubViews,
      view.DrawingSubViews
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.DrawingText, view.DrawingText)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.DrewText, view.DrewText)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.EnabledChanged, view.EnabledChanged)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.FocusedChanged, view.FocusedChanged)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.FrameChanged, view.FrameChanged)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.GettingAttributeForRole,
      view.GettingAttributeForRole
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.GettingScheme, view.GettingScheme)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.HandlingHotKey, view.HandlingHotKey)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.HasFocusChanged,
      view.HasFocusChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.HasFocusChanging,
      view.HasFocusChanging
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.HeightChanged, view.HeightChanged)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.HeightChanging, view.HeightChanging)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.HotKeyChanged, view.HotKeyChanged)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.HotKeyCommand, view.HotKeyCommand)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.Initialized, view.Initialized)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.KeyDown, view.KeyDown)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.KeyDownNotHandled,
      view.KeyDownNotHandled
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.KeyUp, view.KeyUp)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.MouseEnter, view.MouseEnter)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.MouseEvent, view.MouseEvent)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.MouseHoldRepeatChanged,
      view.MouseHoldRepeatChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.MouseHoldRepeatChanging,
      view.MouseHoldRepeatChanging
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.MouseLeave, view.MouseLeave)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.MouseStateChanged,
      view.MouseStateChanged
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.Removed, view.Removed)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.SchemeChanged, view.SchemeChanged)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.SchemeChanging, view.SchemeChanging)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.SchemeNameChanged,
      view.SchemeNameChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.SchemeNameChanging,
      view.SchemeNameChanging
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.SubViewAdded, view.SubViewAdded)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.SubViewLayout, view.SubViewLayout)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.SubViewRemoved, view.SubViewRemoved)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.SubViewsLaidOut,
      view.SubViewsLaidOut
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.SuperViewChanged,
      view.SuperViewChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.SuperViewChanging,
      view.SuperViewChanging
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.TextChanged, view.TextChanged)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.TitleChanged, view.TitleChanged)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.TitleChanging, view.TitleChanging)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.ViewportChanged,
      view.ViewportChanged
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.VisibleChanged, view.VisibleChanged)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.View.VisibleChanging,
      view.VisibleChanging
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.WidthChanged, view.WidthChanged)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.View.WidthChanging, view.WidthChanging)


  let removeProps (domNode: DomNode, props: Props) =
    let view = domNode.View

    // Properties
    props
    |> Props.tryFind PKey.View.Arrangement
    |> Option.iter (fun _ -> view.Arrangement <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.AssignHotKeys
    |> Option.iter (fun _ -> view.AssignHotKeys <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.BorderStyle
    |> Option.iter (fun _ -> view.BorderStyle <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.CanFocus
    |> Option.iter (fun _ -> view.CanFocus <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.CommandsToBubbleUp
    |> Option.iter (fun _ -> view.CommandsToBubbleUp <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.ContentSizeTracksViewport
    |> Option.iter (fun _ -> view.ContentSizeTracksViewport <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.Cursor
    |> Option.iter (fun _ -> view.Cursor <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.Data
    |> Option.iter (fun _ -> view.Data <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.DefaultAcceptView
    |> Option.iter (fun _ -> view.DefaultAcceptView <- new View())

    props
    |> Props.tryFind PKey.View.Enabled
    |> Option.iter (fun _ -> view.Enabled <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.Frame
    |> Option.iter (fun _ -> view.Frame <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.HasFocus
    |> Option.iter (fun _ -> view.HasFocus <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.Height
    |> Option.iter (fun _ -> view.Height <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.HotKey
    |> Option.iter (fun _ -> view.HotKey <- Terminal.Gui.Input.Key.Empty)

    props
    |> Props.tryFind PKey.View.HotKeySpecifier
    |> Option.iter (fun _ -> view.HotKeySpecifier <- Unchecked.defaultof<_>)

    props |> Props.tryFind PKey.View.Id |> Option.iter (fun _ -> view.Id <- "")

    props
    |> Props.tryFind PKey.View.IsInitialized
    |> Option.iter (fun _ -> view.IsInitialized <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.MouseHighlightStates
    |> Option.iter (fun _ -> view.MouseHighlightStates <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.MouseHoldRepeat
    |> Option.iter (fun _ -> view.MouseHoldRepeat <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.MousePositionTracking
    |> Option.iter (fun _ -> view.MousePositionTracking <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.PreserveTrailingSpaces
    |> Option.iter (fun _ -> view.PreserveTrailingSpaces <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.SchemeName
    |> Option.iter (fun _ -> view.SchemeName <- "")

    props
    |> Props.tryFind PKey.View.ShadowStyle
    |> Option.iter (fun _ -> view.ShadowStyle <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.SuperViewRendersLineCanvas
    |> Option.iter (fun _ -> view.SuperViewRendersLineCanvas <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.TabStop
    |> Option.iter (fun _ -> view.TabStop <- Unchecked.defaultof<_>)

    props |> Props.tryFind PKey.View.Text |> Option.iter (fun _ -> view.Text <- "")

    props
    |> Props.tryFind PKey.View.TextAlignment
    |> Option.iter (fun _ -> view.TextAlignment <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.TextDirection
    |> Option.iter (fun _ -> view.TextDirection <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.Title
    |> Option.iter (fun _ -> view.Title <- "")

    props
    |> Props.tryFind PKey.View.UsedHotKeys
    |> Option.iter (fun _ -> view.UsedHotKeys <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.ValidatePosDim
    |> Option.iter (fun _ -> view.ValidatePosDim <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.VerticalTextAlignment
    |> Option.iter (fun _ -> view.VerticalTextAlignment <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.Viewport
    |> Option.iter (fun _ -> view.Viewport <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.ViewportSettings
    |> Option.iter (fun _ -> view.ViewportSettings <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.Visible
    |> Option.iter (fun _ -> view.Visible <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.View.Width
    |> Option.iter (fun _ -> view.Width <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.Accepted)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.Accepting)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.Activated)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.Activating)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.AdvancingFocus)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.BorderStyleChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.CanFocusChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.ClearedViewport)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.ClearingViewport)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.CommandNotBound)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.ContentSizeChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.ContentSizeChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.Disposing)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.DrawComplete)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.DrawingContent)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.DrawingSubViews)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.DrawingText)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.DrewText)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.EnabledChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.FocusedChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.FrameChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.GettingAttributeForRole)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.GettingScheme)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.HandlingHotKey)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.HasFocusChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.HasFocusChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.HeightChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.HeightChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.HotKeyChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.HotKeyCommand)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.Initialized)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.KeyDown)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.KeyDownNotHandled)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.KeyUp)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.MouseEnter)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.MouseEvent)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.MouseHoldRepeatChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.MouseHoldRepeatChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.MouseLeave)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.MouseStateChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.Removed)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.SchemeChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.SchemeChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.SchemeNameChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.SchemeNameChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.SubViewAdded)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.SubViewLayout)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.SubViewRemoved)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.SubViewsLaidOut)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.SuperViewChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.SuperViewChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.TextChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.TitleChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.TitleChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.ViewportChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.VisibleChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.VisibleChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.WidthChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.View.WidthChanging)

  let newView () : View = new View()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list =
    [ PKey.View.DefaultAcceptView_element.key ]


module internal AdornmentDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> Adornment

    // Properties
    props
    |> Props.tryFind PKey.Adornment.Diagnostics
    |> Option.iter (fun v -> view.Diagnostics <- v)

    props
    |> Props.tryFind PKey.Adornment.Parent
    |> Option.iter (fun v -> view.Parent <- v)

    props
    |> Props.tryFind PKey.Adornment.SuperViewRendersLineCanvas
    |> Option.iter (fun v -> view.SuperViewRendersLineCanvas <- v)

    props
    |> Props.tryFind PKey.Adornment.Thickness
    |> Option.iter (fun v -> view.Thickness <- v)

    props
    |> Props.tryFind PKey.Adornment.Viewport
    |> Option.iter (fun v -> view.Viewport <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.Adornment.ThicknessChanged,
      view.ThicknessChanged
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> Adornment

    // Properties
    props
    |> Props.tryFind PKey.Adornment.Diagnostics
    |> Option.iter (fun _ -> view.Diagnostics <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Adornment.Parent
    |> Option.iter (fun _ -> view.Parent <- new View())

    props
    |> Props.tryFind PKey.Adornment.SuperViewRendersLineCanvas
    |> Option.iter (fun _ -> view.SuperViewRendersLineCanvas <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Adornment.Thickness
    |> Option.iter (fun _ -> view.Thickness <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Adornment.Viewport
    |> Option.iter (fun _ -> view.Viewport <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Adornment.ThicknessChanged)

  let newView () : View = new Adornment()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list =
    ViewDom.subElementsPropKeys @ [ PKey.Adornment.Parent_element.key ]


module internal AttributePickerDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> AttributePicker

    // Properties
    props
    |> Props.tryFind PKey.AttributePicker.SampleText
    |> Option.iter (fun v -> view.SampleText <- v)

    props
    |> Props.tryFind PKey.AttributePicker.Value
    |> Option.iter (fun v -> view.Value <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.AttributePicker.ValueChanged,
      view.ValueChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.AttributePicker.ValueChangedUntyped,
      view.ValueChangedUntyped
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.AttributePicker.ValueChanging,
      view.ValueChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> AttributePicker

    // Properties
    props
    |> Props.tryFind PKey.AttributePicker.SampleText
    |> Option.iter (fun _ -> view.SampleText <- "")

    props
    |> Props.tryFind PKey.AttributePicker.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.AttributePicker.ValueChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.AttributePicker.ValueChangedUntyped)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.AttributePicker.ValueChanging)

  let newView () : View = new AttributePicker()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal BarDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> Bar

    // Properties
    props
    |> Props.tryFind PKey.Bar.AlignmentModes
    |> Option.iter (fun v -> view.AlignmentModes <- v)

    props
    |> Props.tryFind PKey.Bar.Orientation
    |> Option.iter (fun v -> view.Orientation <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.Bar.OrientationChanged,
      view.OrientationChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.Bar.OrientationChanging,
      view.OrientationChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> Bar

    // Properties
    props
    |> Props.tryFind PKey.Bar.AlignmentModes
    |> Option.iter (fun _ -> view.AlignmentModes <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Bar.Orientation
    |> Option.iter (fun _ -> view.Orientation <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Bar.OrientationChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Bar.OrientationChanging)

  let newView () : View = new Bar()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal BorderDom =

  let applyProps (domNode: DomNode, props: Props) =
    AdornmentDom.applyProps (domNode, props)
    let view = domNode.View :?> Border

    // Properties
    props
    |> Props.tryFind PKey.Border.LineStyle
    |> Option.iter (fun v -> view.LineStyle <- v)

    props
    |> Props.tryFind PKey.Border.Settings
    |> Option.iter (fun v -> view.Settings <- v)


  let removeProps (domNode: DomNode, props: Props) =
    AdornmentDom.removeProps (domNode, props)
    let view = domNode.View :?> Border

    // Properties
    props
    |> Props.tryFind PKey.Border.LineStyle
    |> Option.iter (fun _ -> view.LineStyle <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Border.Settings
    |> Option.iter (fun _ -> view.Settings <- Unchecked.defaultof<_>)


  let newView () : View = new Border()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = AdornmentDom.subElementsPropKeys


module internal ButtonDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> Button

    // Properties
    props
    |> Props.tryFind PKey.Button.HotKeySpecifier
    |> Option.iter (fun v -> view.HotKeySpecifier <- v)

    props
    |> Props.tryFind PKey.Button.IsDefault
    |> Option.iter (fun v -> view.IsDefault <- v)

    props
    |> Props.tryFind PKey.Button.NoDecorations
    |> Option.iter (fun v -> view.NoDecorations <- v)

    props
    |> Props.tryFind PKey.Button.NoPadding
    |> Option.iter (fun v -> view.NoPadding <- v)

    props |> Props.tryFind PKey.Button.Text |> Option.iter (fun v -> view.Text <- v)


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> Button

    // Properties
    props
    |> Props.tryFind PKey.Button.HotKeySpecifier
    |> Option.iter (fun _ -> view.HotKeySpecifier <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Button.IsDefault
    |> Option.iter (fun _ -> view.IsDefault <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Button.NoDecorations
    |> Option.iter (fun _ -> view.NoDecorations <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Button.NoPadding
    |> Option.iter (fun _ -> view.NoPadding <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Button.Text
    |> Option.iter (fun _ -> view.Text <- "")


  let newView () : View = new Button()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal CharMapDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> CharMap

    // Properties
    props
    |> Props.tryFind PKey.CharMap.SelectedCodePoint
    |> Option.iter (fun v -> view.SelectedCodePoint <- v)

    props
    |> Props.tryFind PKey.CharMap.ShowGlyphWidths
    |> Option.iter (fun v -> view.ShowGlyphWidths <- v)

    props
    |> Props.tryFind PKey.CharMap.ShowUnicodeCategory
    |> Option.iter (fun v -> view.ShowUnicodeCategory <- v)

    props
    |> Props.tryFind PKey.CharMap.StartCodePoint
    |> Option.iter (fun v -> view.StartCodePoint <- v)

    props
    |> Props.tryFind PKey.CharMap.Value
    |> Option.iter (fun v -> view.Value <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.CharMap.ValueChanged, view.ValueChanged)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.CharMap.ValueChangedUntyped,
      view.ValueChangedUntyped
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.CharMap.ValueChanging,
      view.ValueChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> CharMap

    // Properties
    props
    |> Props.tryFind PKey.CharMap.SelectedCodePoint
    |> Option.iter (fun _ -> view.SelectedCodePoint <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.CharMap.ShowGlyphWidths
    |> Option.iter (fun _ -> view.ShowGlyphWidths <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.CharMap.ShowUnicodeCategory
    |> Option.iter (fun _ -> view.ShowUnicodeCategory <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.CharMap.StartCodePoint
    |> Option.iter (fun _ -> view.StartCodePoint <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.CharMap.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.CharMap.ValueChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.CharMap.ValueChangedUntyped)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.CharMap.ValueChanging)

  let newView () : View = new CharMap()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal CheckBoxDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> CheckBox

    // Properties
    props
    |> Props.tryFind PKey.CheckBox.AllowCheckStateNone
    |> Option.iter (fun v -> view.AllowCheckStateNone <- v)

    props
    |> Props.tryFind PKey.CheckBox.HotKeySpecifier
    |> Option.iter (fun v -> view.HotKeySpecifier <- v)

    props
    |> Props.tryFind PKey.CheckBox.RadioStyle
    |> Option.iter (fun v -> view.RadioStyle <- v)

    props
    |> Props.tryFind PKey.CheckBox.Text
    |> Option.iter (fun v -> view.Text <- v)

    props
    |> Props.tryFind PKey.CheckBox.Value
    |> Option.iter (fun v -> view.Value <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.CheckBox.ValueChanged, view.ValueChanged)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.CheckBox.ValueChangedUntyped,
      view.ValueChangedUntyped
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.CheckBox.ValueChanging,
      view.ValueChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> CheckBox

    // Properties
    props
    |> Props.tryFind PKey.CheckBox.AllowCheckStateNone
    |> Option.iter (fun _ -> view.AllowCheckStateNone <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.CheckBox.HotKeySpecifier
    |> Option.iter (fun _ -> view.HotKeySpecifier <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.CheckBox.RadioStyle
    |> Option.iter (fun _ -> view.RadioStyle <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.CheckBox.Text
    |> Option.iter (fun _ -> view.Text <- "")

    props
    |> Props.tryFind PKey.CheckBox.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.CheckBox.ValueChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.CheckBox.ValueChangedUntyped)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.CheckBox.ValueChanging)

  let newView () : View = new CheckBox()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal ColorPickerDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> ColorPicker

    // Properties
    props
    |> Props.tryFind PKey.ColorPicker.SelectedColor
    |> Option.iter (fun v -> view.SelectedColor <- v)

    props
    |> Props.tryFind PKey.ColorPicker.Style
    |> Option.iter (fun v -> view.Style <- v)

    props
    |> Props.tryFind PKey.ColorPicker.Text
    |> Option.iter (fun v -> view.Text <- v)

    props
    |> Props.tryFind PKey.ColorPicker.Value
    |> Option.iter (fun v -> view.Value <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ColorPicker.ValueChanged,
      view.ValueChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ColorPicker.ValueChangedUntyped,
      view.ValueChangedUntyped
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ColorPicker.ValueChanging,
      view.ValueChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> ColorPicker

    // Properties
    props
    |> Props.tryFind PKey.ColorPicker.SelectedColor
    |> Option.iter (fun _ -> view.SelectedColor <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ColorPicker.Style
    |> Option.iter (fun _ -> view.Style <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ColorPicker.Text
    |> Option.iter (fun _ -> view.Text <- "")

    props
    |> Props.tryFind PKey.ColorPicker.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ColorPicker.ValueChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ColorPicker.ValueChangedUntyped)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ColorPicker.ValueChanging)

  let newView () : View = new ColorPicker()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal ColorPicker16Dom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> ColorPicker16

    // Properties
    props
    |> Props.tryFind PKey.ColorPicker16.BoxHeight
    |> Option.iter (fun v -> view.BoxHeight <- v)

    props
    |> Props.tryFind PKey.ColorPicker16.BoxWidth
    |> Option.iter (fun v -> view.BoxWidth <- v)

    props
    |> Props.tryFind PKey.ColorPicker16.Caret
    |> Option.iter (fun v -> view.Caret <- v)

    props
    |> Props.tryFind PKey.ColorPicker16.SelectedColor
    |> Option.iter (fun v -> view.SelectedColor <- v)

    props
    |> Props.tryFind PKey.ColorPicker16.Value
    |> Option.iter (fun v -> view.Value <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ColorPicker16.ValueChanged,
      view.ValueChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ColorPicker16.ValueChangedUntyped,
      view.ValueChangedUntyped
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ColorPicker16.ValueChanging,
      view.ValueChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> ColorPicker16

    // Properties
    props
    |> Props.tryFind PKey.ColorPicker16.BoxHeight
    |> Option.iter (fun _ -> view.BoxHeight <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ColorPicker16.BoxWidth
    |> Option.iter (fun _ -> view.BoxWidth <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ColorPicker16.Caret
    |> Option.iter (fun _ -> view.Caret <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ColorPicker16.SelectedColor
    |> Option.iter (fun _ -> view.SelectedColor <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ColorPicker16.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ColorPicker16.ValueChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ColorPicker16.ValueChangedUntyped)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ColorPicker16.ValueChanging)

  let newView () : View = new ColorPicker16()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal DatePickerDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> DatePicker

    // Properties
    props
    |> Props.tryFind PKey.DatePicker.Culture
    |> Option.iter (fun v -> view.Culture <- v)

    props
    |> Props.tryFind PKey.DatePicker.Text
    |> Option.iter (fun v -> view.Text <- v)

    props
    |> Props.tryFind PKey.DatePicker.Value
    |> Option.iter (fun v -> view.Value <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.DatePicker.ValueChanged,
      view.ValueChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.DatePicker.ValueChangedUntyped,
      view.ValueChangedUntyped
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.DatePicker.ValueChanging,
      view.ValueChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> DatePicker

    // Properties
    props
    |> Props.tryFind PKey.DatePicker.Culture
    |> Option.iter (fun _ -> view.Culture <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.DatePicker.Text
    |> Option.iter (fun _ -> view.Text <- "")

    props
    |> Props.tryFind PKey.DatePicker.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.DatePicker.ValueChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.DatePicker.ValueChangedUntyped)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.DatePicker.ValueChanging)

  let newView () : View = new DatePicker()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal FrameViewDom =

  let applyProps (domNode: DomNode, props: Props) = ViewDom.applyProps (domNode, props)

  let removeProps (domNode: DomNode, props: Props) = ViewDom.removeProps (domNode, props)

  let newView () : View = new FrameView()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal GraphViewDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> GraphView

    // Properties
    props
    |> Props.tryFind PKey.GraphView.AxisX
    |> Option.iter (fun v -> view.AxisX <- v)

    props
    |> Props.tryFind PKey.GraphView.AxisY
    |> Option.iter (fun v -> view.AxisY <- v)

    props
    |> Props.tryFind PKey.GraphView.CellSize
    |> Option.iter (fun v -> view.CellSize <- v)

    props
    |> Props.tryFind PKey.GraphView.GraphColor
    |> Option.iter (fun v -> view.GraphColor <- v)

    props
    |> Props.tryFind PKey.GraphView.MarginBottom
    |> Option.iter (fun v -> view.MarginBottom <- v)

    props
    |> Props.tryFind PKey.GraphView.MarginLeft
    |> Option.iter (fun v -> view.MarginLeft <- v)

    props
    |> Props.tryFind PKey.GraphView.ScrollOffset
    |> Option.iter (fun v -> view.ScrollOffset <- v)


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> GraphView

    // Properties
    props
    |> Props.tryFind PKey.GraphView.AxisX
    |> Option.iter (fun _ -> view.AxisX <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.GraphView.AxisY
    |> Option.iter (fun _ -> view.AxisY <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.GraphView.CellSize
    |> Option.iter (fun _ -> view.CellSize <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.GraphView.GraphColor
    |> Option.iter (fun _ -> view.GraphColor <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.GraphView.MarginBottom
    |> Option.iter (fun _ -> view.MarginBottom <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.GraphView.MarginLeft
    |> Option.iter (fun _ -> view.MarginLeft <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.GraphView.ScrollOffset
    |> Option.iter (fun _ -> view.ScrollOffset <- Unchecked.defaultof<_>)


  let newView () : View = new GraphView()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal HexViewDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> HexView

    // Properties
    props
    |> Props.tryFind PKey.HexView.Address
    |> Option.iter (fun v -> view.Address <- v)

    props
    |> Props.tryFind PKey.HexView.AddressWidth
    |> Option.iter (fun v -> view.AddressWidth <- v)

    props
    |> Props.tryFind PKey.HexView.BytesPerLine
    |> Option.iter (fun v -> view.BytesPerLine <- v)

    props
    |> Props.tryFind PKey.HexView.ReadOnly
    |> Option.iter (fun v -> view.ReadOnly <- v)

    props
    |> Props.tryFind PKey.HexView.Source
    |> Option.iter (fun v -> view.Source <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.HexView.Edited, view.Edited)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.HexView.PositionChanged,
      view.PositionChanged
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> HexView

    // Properties
    props
    |> Props.tryFind PKey.HexView.Address
    |> Option.iter (fun _ -> view.Address <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.HexView.AddressWidth
    |> Option.iter (fun _ -> view.AddressWidth <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.HexView.BytesPerLine
    |> Option.iter (fun _ -> view.BytesPerLine <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.HexView.ReadOnly
    |> Option.iter (fun _ -> view.ReadOnly <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.HexView.Source
    |> Option.iter (fun _ -> view.Source <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.HexView.Edited)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.HexView.PositionChanged)

  let newView () : View = new HexView()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal LabelDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> Label

    // Properties
    props
    |> Props.tryFind PKey.Label.HotKeySpecifier
    |> Option.iter (fun v -> view.HotKeySpecifier <- v)

    props |> Props.tryFind PKey.Label.Text |> Option.iter (fun v -> view.Text <- v)


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> Label

    // Properties
    props
    |> Props.tryFind PKey.Label.HotKeySpecifier
    |> Option.iter (fun _ -> view.HotKeySpecifier <- Unchecked.defaultof<_>)

    props |> Props.tryFind PKey.Label.Text |> Option.iter (fun _ -> view.Text <- "")


  let newView () : View = new Label()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal LegendAnnotationDom =

  let applyProps (domNode: DomNode, props: Props) = ViewDom.applyProps (domNode, props)

  let removeProps (domNode: DomNode, props: Props) = ViewDom.removeProps (domNode, props)

  let newView () : View = new LegendAnnotation()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal LineDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> Line

    // Properties
    props
    |> Props.tryFind PKey.Line.Length
    |> Option.iter (fun v -> view.Length <- v)

    props
    |> Props.tryFind PKey.Line.Orientation
    |> Option.iter (fun v -> view.Orientation <- v)

    props |> Props.tryFind PKey.Line.Style |> Option.iter (fun v -> view.Style <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.Line.OrientationChanged,
      view.OrientationChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.Line.OrientationChanging,
      view.OrientationChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> Line

    // Properties
    props
    |> Props.tryFind PKey.Line.Length
    |> Option.iter (fun _ -> view.Length <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Line.Orientation
    |> Option.iter (fun _ -> view.Orientation <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Line.Style
    |> Option.iter (fun _ -> view.Style <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Line.OrientationChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Line.OrientationChanging)

  let newView () : View = new Line()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal LinearRangeDom =

  let applyProps<'T> (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> LinearRange<'T>

    // Properties
    props
    |> Props.tryFind PKey.LinearRange<'T>.AllowEmpty
    |> Option.iter (fun v -> view.AllowEmpty <- v)

    props
    |> Props.tryFind PKey.LinearRange<'T>.FocusedOption
    |> Option.iter (fun v -> view.FocusedOption <- v)

    props
    |> Props.tryFind PKey.LinearRange<'T>.LegendsOrientation
    |> Option.iter (fun v -> view.LegendsOrientation <- v)

    props
    |> Props.tryFind PKey.LinearRange<'T>.MinimumInnerSpacing
    |> Option.iter (fun v -> view.MinimumInnerSpacing <- v)

    props
    |> Props.tryFind PKey.LinearRange<'T>.Options
    |> Option.iter (fun v -> view.Options <- v)

    props
    |> Props.tryFind PKey.LinearRange<'T>.Orientation
    |> Option.iter (fun v -> view.Orientation <- v)

    props
    |> Props.tryFind PKey.LinearRange<'T>.RangeAllowSingle
    |> Option.iter (fun v -> view.RangeAllowSingle <- v)

    props
    |> Props.tryFind PKey.LinearRange<'T>.ShowEndSpacing
    |> Option.iter (fun v -> view.ShowEndSpacing <- v)

    props
    |> Props.tryFind PKey.LinearRange<'T>.ShowLegends
    |> Option.iter (fun v -> view.ShowLegends <- v)

    props
    |> Props.tryFind PKey.LinearRange<'T>.Style
    |> Option.iter (fun v -> view.Style <- v)

    props
    |> Props.tryFind PKey.LinearRange<'T>.Text
    |> Option.iter (fun v -> view.Text <- v)

    props
    |> Props.tryFind PKey.LinearRange<'T>.Type
    |> Option.iter (fun v -> view.Type <- v)

    props
    |> Props.tryFind PKey.LinearRange<'T>.UseMinimumSize
    |> Option.iter (fun v -> view.UseMinimumSize <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.LegendsOrientationChanged,
      view.LegendsOrientationChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.LegendsOrientationChanging,
      view.LegendsOrientationChanging
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.MinimumInnerSpacingChanged,
      view.MinimumInnerSpacingChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.MinimumInnerSpacingChanging,
      view.MinimumInnerSpacingChanging
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.OptionFocused,
      view.OptionFocused
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.OptionsChanged,
      view.OptionsChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.OrientationChanged,
      view.OrientationChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.OrientationChanging,
      view.OrientationChanging
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.ShowEndSpacingChanged,
      view.ShowEndSpacingChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.ShowEndSpacingChanging,
      view.ShowEndSpacingChanging
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.ShowLegendsChanged,
      view.ShowLegendsChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.ShowLegendsChanging,
      view.ShowLegendsChanging
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.TypeChanged,
      view.TypeChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.TypeChanging,
      view.TypeChanging
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.UseMinimumSizeChanged,
      view.UseMinimumSizeChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.LinearRange<'T>.UseMinimumSizeChanging,
      view.UseMinimumSizeChanging
    )


  let removeProps<'T> (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> LinearRange<'T>

    // Properties
    props
    |> Props.tryFind PKey.LinearRange<'T>.AllowEmpty
    |> Option.iter (fun _ -> view.AllowEmpty <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.LinearRange<'T>.FocusedOption
    |> Option.iter (fun _ -> view.FocusedOption <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.LinearRange<'T>.LegendsOrientation
    |> Option.iter (fun _ -> view.LegendsOrientation <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.LinearRange<'T>.MinimumInnerSpacing
    |> Option.iter (fun _ -> view.MinimumInnerSpacing <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.LinearRange<'T>.Options
    |> Option.iter (fun _ -> view.Options <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.LinearRange<'T>.Orientation
    |> Option.iter (fun _ -> view.Orientation <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.LinearRange<'T>.RangeAllowSingle
    |> Option.iter (fun _ -> view.RangeAllowSingle <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.LinearRange<'T>.ShowEndSpacing
    |> Option.iter (fun _ -> view.ShowEndSpacing <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.LinearRange<'T>.ShowLegends
    |> Option.iter (fun _ -> view.ShowLegends <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.LinearRange<'T>.Style
    |> Option.iter (fun _ -> view.Style <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.LinearRange<'T>.Text
    |> Option.iter (fun _ -> view.Text <- "")

    props
    |> Props.tryFind PKey.LinearRange<'T>.Type
    |> Option.iter (fun _ -> view.Type <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.LinearRange<'T>.UseMinimumSize
    |> Option.iter (fun _ -> view.UseMinimumSize <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.LegendsOrientationChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.LegendsOrientationChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.MinimumInnerSpacingChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.MinimumInnerSpacingChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.OptionFocused)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.OptionsChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.OrientationChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.OrientationChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.ShowEndSpacingChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.ShowEndSpacingChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.ShowLegendsChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.ShowLegendsChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.TypeChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.TypeChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.UseMinimumSizeChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.LinearRange<'T>.UseMinimumSizeChanging)

  let newView<'T> () : View = new LinearRange<'T>()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal LinearRange'Dom =

  let applyProps (domNode: DomNode, props: Props) =
    LinearRangeDom.applyProps<System.Object> (domNode, props)

  let removeProps (domNode: DomNode, props: Props) =
    LinearRangeDom.removeProps<System.Object> (domNode, props)

  let newView () : View = new LinearRange()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = LinearRangeDom.subElementsPropKeys


module internal LinkDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> Link

    // Properties
    props |> Props.tryFind PKey.Link.Url |> Option.iter (fun v -> view.Url <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.Link.UrlChanged, view.UrlChanged)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.Link.UrlChanging, view.UrlChanging)


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> Link

    // Properties
    props |> Props.tryFind PKey.Link.Url |> Option.iter (fun _ -> view.Url <- "")

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Link.UrlChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Link.UrlChanging)

  let newView () : View = new Link()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal ListViewDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> ListView

    // Properties
    props
    |> Props.tryFind PKey.ListView.MarkMultiple
    |> Option.iter (fun v -> view.MarkMultiple <- v)

    props
    |> Props.tryFind PKey.ListView.SelectedItem
    |> Option.iter (fun v -> view.SelectedItem <- v)

    props
    |> Props.tryFind PKey.ListView.ShowMarks
    |> Option.iter (fun v -> view.ShowMarks <- v)

    props
    |> Props.tryFind PKey.ListView.Source
    |> Option.iter (fun v -> view.Source <- v)

    props
    |> Props.tryFind PKey.ListView.Value
    |> Option.iter (fun v -> view.Value <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ListView.CollectionChanged,
      view.CollectionChanged
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.ListView.RowRender, view.RowRender)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ListView.SourceChanged,
      view.SourceChanged
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.ListView.ValueChanged, view.ValueChanged)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ListView.ValueChangedUntyped,
      view.ValueChangedUntyped
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ListView.ValueChanging,
      view.ValueChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> ListView

    // Properties
    props
    |> Props.tryFind PKey.ListView.MarkMultiple
    |> Option.iter (fun _ -> view.MarkMultiple <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ListView.SelectedItem
    |> Option.iter (fun _ -> view.SelectedItem <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ListView.ShowMarks
    |> Option.iter (fun _ -> view.ShowMarks <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ListView.Source
    |> Option.iter (fun _ -> view.Source <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ListView.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ListView.CollectionChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ListView.RowRender)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ListView.SourceChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ListView.ValueChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ListView.ValueChangedUntyped)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ListView.ValueChanging)

  let newView () : View = new ListView()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal MarginDom =

  let applyProps (domNode: DomNode, props: Props) =
    AdornmentDom.applyProps (domNode, props)
    let view = domNode.View :?> Margin

    // Properties
    props
    |> Props.tryFind PKey.Margin.ShadowSize
    |> Option.iter (fun v -> view.ShadowSize <- v)

    props
    |> Props.tryFind PKey.Margin.ShadowStyle
    |> Option.iter (fun v -> view.ShadowStyle <- v)


  let removeProps (domNode: DomNode, props: Props) =
    AdornmentDom.removeProps (domNode, props)
    let view = domNode.View :?> Margin

    // Properties
    props
    |> Props.tryFind PKey.Margin.ShadowSize
    |> Option.iter (fun _ -> view.ShadowSize <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Margin.ShadowStyle
    |> Option.iter (fun _ -> view.ShadowStyle <- Unchecked.defaultof<_>)


  let newView () : View = new Margin()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = AdornmentDom.subElementsPropKeys


module internal MenuDom =

  let applyProps (domNode: DomNode, props: Props) =
    BarDom.applyProps (domNode, props)
    let view = domNode.View :?> Menu

    // Properties
    props
    |> Props.tryFind PKey.Menu.SuperMenuItem
    |> Option.iter (fun v -> view.SuperMenuItem <- v)

    props |> Props.tryFind PKey.Menu.Value |> Option.iter (fun v -> view.Value <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.Menu.SelectedMenuItemChanged,
      view.SelectedMenuItemChanged
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.Menu.ValueChanged, view.ValueChanged)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.Menu.ValueChangedUntyped,
      view.ValueChangedUntyped
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.Menu.ValueChanging, view.ValueChanging)


  let removeProps (domNode: DomNode, props: Props) =
    BarDom.removeProps (domNode, props)
    let view = domNode.View :?> Menu

    // Properties
    props
    |> Props.tryFind PKey.Menu.SuperMenuItem
    |> Option.iter (fun _ -> view.SuperMenuItem <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Menu.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Menu.SelectedMenuItemChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Menu.ValueChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Menu.ValueChangedUntyped)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Menu.ValueChanging)

  let newView () : View = new Menu()

  let setAsChildOfParentView: bool = false

  let subElementsPropKeys: RawPropKey list =
    BarDom.subElementsPropKeys
    @ [ PKey.Menu.SuperMenuItem_element.key; PKey.Menu.Value_element.key ]


module internal MenuBarDom =

  let applyProps (domNode: DomNode, props: Props) =
    MenuDom.applyProps (domNode, props)
    let view = domNode.View :?> MenuBar

    // Properties
    props |> Props.tryFind PKey.MenuBar.Key |> Option.iter (fun v -> view.Key <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.MenuBar.KeyChanged, view.KeyChanged)


  let removeProps (domNode: DomNode, props: Props) =
    MenuDom.removeProps (domNode, props)
    let view = domNode.View :?> MenuBar

    // Properties
    props
    |> Props.tryFind PKey.MenuBar.Key
    |> Option.iter (fun _ -> view.Key <- Terminal.Gui.Input.Key.Empty)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.MenuBar.KeyChanged)

  let newView () : View = new MenuBar()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = MenuDom.subElementsPropKeys


module internal NumericUpDownDom =

  let applyProps<'T> (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> NumericUpDown<'T>

    // Properties
    props
    |> Props.tryFind PKey.NumericUpDown<'T>.Format
    |> Option.iter (fun v -> view.Format <- v)

    props
    |> Props.tryFind PKey.NumericUpDown<'T>.Increment
    |> Option.iter (fun v -> view.Increment <- v)

    props
    |> Props.tryFind PKey.NumericUpDown<'T>.Value
    |> Option.iter (fun v -> view.Value <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.NumericUpDown<'T>.FormatChanged,
      view.FormatChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.NumericUpDown<'T>.IncrementChanged,
      view.IncrementChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.NumericUpDown<'T>.ValueChanged,
      view.ValueChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.NumericUpDown<'T>.ValueChangedUntyped,
      view.ValueChangedUntyped
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.NumericUpDown<'T>.ValueChanging,
      view.ValueChanging
    )


  let removeProps<'T> (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> NumericUpDown<'T>

    // Properties
    props
    |> Props.tryFind PKey.NumericUpDown<'T>.Format
    |> Option.iter (fun _ -> view.Format <- "")

    props
    |> Props.tryFind PKey.NumericUpDown<'T>.Increment
    |> Option.iter (fun _ -> view.Increment <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.NumericUpDown<'T>.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.NumericUpDown<'T>.FormatChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.NumericUpDown<'T>.IncrementChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.NumericUpDown<'T>.ValueChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.NumericUpDown<'T>.ValueChangedUntyped)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.NumericUpDown<'T>.ValueChanging)

  let newView<'T> () : View = new NumericUpDown<'T>()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal NumericUpDown'Dom =

  let applyProps (domNode: DomNode, props: Props) =
    NumericUpDownDom.applyProps<int> (domNode, props)

  let removeProps (domNode: DomNode, props: Props) =
    NumericUpDownDom.removeProps<int> (domNode, props)

  let newView () : View = new NumericUpDown()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = NumericUpDownDom.subElementsPropKeys


module internal PaddingDom =

  let applyProps (domNode: DomNode, props: Props) =
    AdornmentDom.applyProps (domNode, props)

  let removeProps (domNode: DomNode, props: Props) =
    AdornmentDom.removeProps (domNode, props)

  let newView () : View = new Padding()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = AdornmentDom.subElementsPropKeys


module internal PopoverImplDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> PopoverImpl

    // Properties
    props
    |> Props.tryFind PKey.PopoverImpl.Anchor
    |> Option.iter (fun v -> view.Anchor <- v)

    props
    |> Props.tryFind PKey.PopoverImpl.Owner
    |> Option.iter (fun v -> view.Owner <- v)

    props
    |> Props.tryFind PKey.PopoverImpl.Target
    |> Option.iter (fun v -> view.Target <- v)


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> PopoverImpl

    // Properties
    props
    |> Props.tryFind PKey.PopoverImpl.Anchor
    |> Option.iter (fun _ -> view.Anchor <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.PopoverImpl.Owner
    |> Option.iter (fun _ -> view.Owner <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.PopoverImpl.Target
    |> Option.iter (fun _ -> view.Target <- Unchecked.defaultof<_>)


  let newView () : View =
    failwith "Cannot instantiate abstract view type PopoverImpl"

  let setAsChildOfParentView: bool = false

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal PopoverDom =

  let applyProps<'TView, 'TResult when 'TView: (new: unit -> 'TView) and 'TView :> Terminal.Gui.ViewBase.View>
    (domNode: DomNode, props: Props)
    =
    PopoverImplDom.applyProps (domNode, props)
    let view = domNode.View :?> Popover<'TView, 'TResult>

    // Properties
    props
    |> Props.tryFind PKey.Popover<'TView, 'TResult>.ContentView
    |> Option.iter (fun v -> view.ContentView <- v)

    props
    |> Props.tryFind PKey.Popover<'TView, 'TResult>.ResultExtractor
    |> Option.iter (fun v -> view.ResultExtractor <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.Popover<'TView, 'TResult>.ResultChanged,
      view.ResultChanged
    )


  let removeProps<'TView, 'TResult when 'TView: (new: unit -> 'TView) and 'TView :> Terminal.Gui.ViewBase.View>
    (domNode: DomNode, props: Props)
    =
    PopoverImplDom.removeProps (domNode, props)
    let view = domNode.View :?> Popover<'TView, 'TResult>

    // Properties
    props
    |> Props.tryFind PKey.Popover<'TView, 'TResult>.ContentView
    |> Option.iter (fun _ -> view.ContentView <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Popover<'TView, 'TResult>.ResultExtractor
    |> Option.iter (fun _ -> view.ResultExtractor <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Popover<'TView, 'TResult>.ResultChanged)

  let newView<'TView, 'TResult when 'TView: (new: unit -> 'TView) and 'TView :> Terminal.Gui.ViewBase.View> () : View =
    new Popover<'TView, 'TResult>()

  let setAsChildOfParentView: bool = false

  let subElementsPropKeys: RawPropKey list =
    PopoverImplDom.subElementsPropKeys
    @ [ PKey.Popover<'TView, 'TResult>.ContentView_element.key ]


module internal PopoverMenuDom =

  let applyProps (domNode: DomNode, props: Props) =
    PopoverDom.applyProps<Terminal.Gui.Views.Menu, Terminal.Gui.Views.MenuItem> (domNode, props)
    let view = domNode.View :?> PopoverMenu

    // Properties
    props
    |> Props.tryFind PKey.PopoverMenu.Key
    |> Option.iter (fun v -> view.Key <- v)

    props
    |> Props.tryFind PKey.PopoverMenu.MouseFlags
    |> Option.iter (fun v -> view.MouseFlags <- v)

    props
    |> Props.tryFind PKey.PopoverMenu.Root
    |> Option.iter (fun v -> view.Root <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.PopoverMenu.KeyChanged, view.KeyChanged)


  let removeProps (domNode: DomNode, props: Props) =
    PopoverDom.removeProps<Terminal.Gui.Views.Menu, Terminal.Gui.Views.MenuItem> (domNode, props)
    let view = domNode.View :?> PopoverMenu

    // Properties
    props
    |> Props.tryFind PKey.PopoverMenu.Key
    |> Option.iter (fun _ -> view.Key <- Terminal.Gui.Input.Key.Empty)

    props
    |> Props.tryFind PKey.PopoverMenu.MouseFlags
    |> Option.iter (fun _ -> view.MouseFlags <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.PopoverMenu.Root
    |> Option.iter (fun _ -> view.Root <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.PopoverMenu.KeyChanged)

  let newView () : View = new PopoverMenu()

  let setAsChildOfParentView: bool = false

  let subElementsPropKeys: RawPropKey list =
    PopoverDom.subElementsPropKeys @ [ PKey.PopoverMenu.Root_element.key ]


module internal ProgressBarDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> ProgressBar

    // Properties
    props
    |> Props.tryFind PKey.ProgressBar.BidirectionalMarquee
    |> Option.iter (fun v -> view.BidirectionalMarquee <- v)

    props
    |> Props.tryFind PKey.ProgressBar.Fraction
    |> Option.iter (fun v -> view.Fraction <- v)

    props
    |> Props.tryFind PKey.ProgressBar.ProgressBarFormat
    |> Option.iter (fun v -> view.ProgressBarFormat <- v)

    props
    |> Props.tryFind PKey.ProgressBar.ProgressBarStyle
    |> Option.iter (fun v -> view.ProgressBarStyle <- v)

    props
    |> Props.tryFind PKey.ProgressBar.SegmentCharacter
    |> Option.iter (fun v -> view.SegmentCharacter <- v)

    props
    |> Props.tryFind PKey.ProgressBar.Text
    |> Option.iter (fun v -> view.Text <- v)


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> ProgressBar

    // Properties
    props
    |> Props.tryFind PKey.ProgressBar.BidirectionalMarquee
    |> Option.iter (fun _ -> view.BidirectionalMarquee <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ProgressBar.Fraction
    |> Option.iter (fun _ -> view.Fraction <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ProgressBar.ProgressBarFormat
    |> Option.iter (fun _ -> view.ProgressBarFormat <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ProgressBar.ProgressBarStyle
    |> Option.iter (fun _ -> view.ProgressBarStyle <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ProgressBar.SegmentCharacter
    |> Option.iter (fun _ -> view.SegmentCharacter <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ProgressBar.Text
    |> Option.iter (fun _ -> view.Text <- "")


  let newView () : View = new ProgressBar()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal RunnableDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> Runnable

    // Properties
    props
    |> Props.tryFind PKey.Runnable.Result
    |> Option.iter (fun v -> view.Result <- v)

    props
    |> Props.tryFind PKey.Runnable.StopRequested
    |> Option.iter (fun v -> view.StopRequested <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.Runnable.IsModalChanged,
      view.IsModalChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.Runnable.IsRunningChanged,
      view.IsRunningChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.Runnable.IsRunningChanging,
      view.IsRunningChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> Runnable

    // Properties
    props
    |> Props.tryFind PKey.Runnable.Result
    |> Option.iter (fun _ -> view.Result <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Runnable.StopRequested
    |> Option.iter (fun _ -> view.StopRequested <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Runnable.IsModalChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Runnable.IsRunningChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Runnable.IsRunningChanging)

  let newView () : View = new Runnable()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal Runnable'Dom =

  let applyProps<'TResult> (domNode: DomNode, props: Props) =
    RunnableDom.applyProps (domNode, props)
    let view = domNode.View :?> Runnable<'TResult>

    // Properties
    props
    |> Props.tryFind PKey.Runnable'<'TResult>.Result
    |> Option.iter (fun v -> view.Result <- v)


  let removeProps<'TResult> (domNode: DomNode, props: Props) =
    RunnableDom.removeProps (domNode, props)
    let view = domNode.View :?> Runnable<'TResult>

    // Properties
    props
    |> Props.tryFind PKey.Runnable'<'TResult>.Result
    |> Option.iter (fun _ -> view.Result <- Unchecked.defaultof<_>)


  let newView<'TResult> () : View = new Runnable<'TResult>()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = RunnableDom.subElementsPropKeys


module internal DialogDom =

  let applyProps<'TResult> (domNode: DomNode, props: Props) =
    Runnable'Dom.applyProps<'TResult> (domNode, props)
    let view = domNode.View :?> Dialog<'TResult>

    // Properties
    props
    |> Props.tryFind PKey.Dialog<'TResult>.ButtonAlignment
    |> Option.iter (fun v -> view.ButtonAlignment <- v)

    props
    |> Props.tryFind PKey.Dialog<'TResult>.ButtonAlignmentModes
    |> Option.iter (fun v -> view.ButtonAlignmentModes <- v)

    props
    |> Props.tryFind PKey.Dialog<'TResult>.Buttons
    |> Option.iter (fun v -> view.Buttons <- v)


  let removeProps<'TResult> (domNode: DomNode, props: Props) =
    Runnable'Dom.removeProps<'TResult> (domNode, props)
    let view = domNode.View :?> Dialog<'TResult>

    // Properties
    props
    |> Props.tryFind PKey.Dialog<'TResult>.ButtonAlignment
    |> Option.iter (fun _ -> view.ButtonAlignment <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Dialog<'TResult>.ButtonAlignmentModes
    |> Option.iter (fun _ -> view.ButtonAlignmentModes <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Dialog<'TResult>.Buttons
    |> Option.iter (fun _ -> view.Buttons <- Unchecked.defaultof<_>)


  let newView<'TResult> () : View = new Dialog<'TResult>()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = Runnable'Dom.subElementsPropKeys


module internal Dialog'Dom =

  let applyProps (domNode: DomNode, props: Props) =
    DialogDom.applyProps<int> (domNode, props)
    let view = domNode.View :?> Dialog

    // Properties
    props
    |> Props.tryFind PKey.Dialog'.Result
    |> Option.iter (fun v -> view.Result <- v)


  let removeProps (domNode: DomNode, props: Props) =
    DialogDom.removeProps<int> (domNode, props)
    let view = domNode.View :?> Dialog

    // Properties
    props
    |> Props.tryFind PKey.Dialog'.Result
    |> Option.iter (fun _ -> view.Result <- Unchecked.defaultof<_>)


  let newView () : View = new Dialog()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = DialogDom.subElementsPropKeys


module internal PromptDom =

  let applyProps<'TView, 'TResult when 'TView: (new: unit -> 'TView) and 'TView :> Terminal.Gui.ViewBase.View>
    (domNode: DomNode, props: Props)
    =
    DialogDom.applyProps<'TResult> (domNode, props)
    let view = domNode.View :?> Prompt<'TView, 'TResult>

    // Properties
    props
    |> Props.tryFind PKey.Prompt<'TView, 'TResult>.ResultExtractor
    |> Option.iter (fun v -> view.ResultExtractor <- v)


  let removeProps<'TView, 'TResult when 'TView: (new: unit -> 'TView) and 'TView :> Terminal.Gui.ViewBase.View>
    (domNode: DomNode, props: Props)
    =
    DialogDom.removeProps<'TResult> (domNode, props)
    let view = domNode.View :?> Prompt<'TView, 'TResult>

    // Properties
    props
    |> Props.tryFind PKey.Prompt<'TView, 'TResult>.ResultExtractor
    |> Option.iter (fun _ -> view.ResultExtractor <- Unchecked.defaultof<_>)


  let newView<'TView, 'TResult when 'TView: (new: unit -> 'TView) and 'TView :> Terminal.Gui.ViewBase.View> () : View =
    new Prompt<'TView, 'TResult>()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = DialogDom.subElementsPropKeys


module internal FileDialogDom =

  let applyProps (domNode: DomNode, props: Props) =
    Dialog'Dom.applyProps (domNode, props)
    let view = domNode.View :?> FileDialog

    // Properties
    props
    |> Props.tryFind PKey.FileDialog.AllowedTypes
    |> Option.iter (fun v -> view.AllowedTypes <- v)

    props
    |> Props.tryFind PKey.FileDialog.AllowsMultipleSelection
    |> Option.iter (fun v -> view.AllowsMultipleSelection <- v)

    props
    |> Props.tryFind PKey.FileDialog.FileOperationsHandler
    |> Option.iter (fun v -> view.FileOperationsHandler <- v)

    props
    |> Props.tryFind PKey.FileDialog.MustExist
    |> Option.iter (fun v -> view.MustExist <- v)

    props
    |> Props.tryFind PKey.FileDialog.OpenMode
    |> Option.iter (fun v -> view.OpenMode <- v)

    props
    |> Props.tryFind PKey.FileDialog.Path
    |> Option.iter (fun v -> view.Path <- v)

    props
    |> Props.tryFind PKey.FileDialog.SearchMatcher
    |> Option.iter (fun v -> view.SearchMatcher <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.FileDialog.FilesSelected,
      view.FilesSelected
    )


  let removeProps (domNode: DomNode, props: Props) =
    Dialog'Dom.removeProps (domNode, props)
    let view = domNode.View :?> FileDialog

    // Properties
    props
    |> Props.tryFind PKey.FileDialog.AllowedTypes
    |> Option.iter (fun _ -> view.AllowedTypes <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.FileDialog.AllowsMultipleSelection
    |> Option.iter (fun _ -> view.AllowsMultipleSelection <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.FileDialog.FileOperationsHandler
    |> Option.iter (fun _ -> view.FileOperationsHandler <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.FileDialog.MustExist
    |> Option.iter (fun _ -> view.MustExist <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.FileDialog.OpenMode
    |> Option.iter (fun _ -> view.OpenMode <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.FileDialog.Path
    |> Option.iter (fun _ -> view.Path <- "")

    props
    |> Props.tryFind PKey.FileDialog.SearchMatcher
    |> Option.iter (fun _ -> view.SearchMatcher <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.FileDialog.FilesSelected)

  let newView () : View = new FileDialog()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = Dialog'Dom.subElementsPropKeys


module internal OpenDialogDom =

  let applyProps (domNode: DomNode, props: Props) =
    FileDialogDom.applyProps (domNode, props)
    let view = domNode.View :?> OpenDialog

    // Properties
    props
    |> Props.tryFind PKey.OpenDialog.OpenMode
    |> Option.iter (fun v -> view.OpenMode <- v)


  let removeProps (domNode: DomNode, props: Props) =
    FileDialogDom.removeProps (domNode, props)
    let view = domNode.View :?> OpenDialog

    // Properties
    props
    |> Props.tryFind PKey.OpenDialog.OpenMode
    |> Option.iter (fun _ -> view.OpenMode <- Unchecked.defaultof<_>)


  let newView () : View = new OpenDialog()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = FileDialogDom.subElementsPropKeys


module internal SaveDialogDom =

  let applyProps (domNode: DomNode, props: Props) =
    FileDialogDom.applyProps (domNode, props)

  let removeProps (domNode: DomNode, props: Props) =
    FileDialogDom.removeProps (domNode, props)

  let newView () : View = new SaveDialog()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = FileDialogDom.subElementsPropKeys


module internal ScrollBarDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> ScrollBar

    // Properties
    props
    |> Props.tryFind PKey.ScrollBar.Increment
    |> Option.iter (fun v -> view.Increment <- v)

    props
    |> Props.tryFind PKey.ScrollBar.Orientation
    |> Option.iter (fun v -> view.Orientation <- v)

    props
    |> Props.tryFind PKey.ScrollBar.ScrollableContentSize
    |> Option.iter (fun v -> view.ScrollableContentSize <- v)

    props
    |> Props.tryFind PKey.ScrollBar.Value
    |> Option.iter (fun v -> view.Value <- v)

    props
    |> Props.tryFind PKey.ScrollBar.VisibilityMode
    |> Option.iter (fun v -> view.VisibilityMode <- v)

    props
    |> Props.tryFind PKey.ScrollBar.VisibleContentSize
    |> Option.iter (fun v -> view.VisibleContentSize <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ScrollBar.OrientationChanged,
      view.OrientationChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ScrollBar.OrientationChanging,
      view.OrientationChanging
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ScrollBar.ScrollableContentSizeChanged,
      view.ScrollableContentSizeChanged
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.ScrollBar.Scrolled, view.Scrolled)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ScrollBar.SliderPositionChanged,
      view.SliderPositionChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ScrollBar.ValueChanged,
      view.ValueChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ScrollBar.ValueChangedUntyped,
      view.ValueChangedUntyped
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ScrollBar.ValueChanging,
      view.ValueChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> ScrollBar

    // Properties
    props
    |> Props.tryFind PKey.ScrollBar.Increment
    |> Option.iter (fun _ -> view.Increment <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ScrollBar.Orientation
    |> Option.iter (fun _ -> view.Orientation <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ScrollBar.ScrollableContentSize
    |> Option.iter (fun _ -> view.ScrollableContentSize <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ScrollBar.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ScrollBar.VisibilityMode
    |> Option.iter (fun _ -> view.VisibilityMode <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ScrollBar.VisibleContentSize
    |> Option.iter (fun _ -> view.VisibleContentSize <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ScrollBar.OrientationChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ScrollBar.OrientationChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ScrollBar.ScrollableContentSizeChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ScrollBar.Scrolled)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ScrollBar.SliderPositionChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ScrollBar.ValueChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ScrollBar.ValueChangedUntyped)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ScrollBar.ValueChanging)

  let newView () : View = new ScrollBar()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal ScrollSliderDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> ScrollSlider

    // Properties
    props
    |> Props.tryFind PKey.ScrollSlider.Orientation
    |> Option.iter (fun v -> view.Orientation <- v)

    props
    |> Props.tryFind PKey.ScrollSlider.Position
    |> Option.iter (fun v -> view.Position <- v)

    props
    |> Props.tryFind PKey.ScrollSlider.Size
    |> Option.iter (fun v -> view.Size <- v)

    props
    |> Props.tryFind PKey.ScrollSlider.SliderPadding
    |> Option.iter (fun v -> view.SliderPadding <- v)

    props
    |> Props.tryFind PKey.ScrollSlider.VisibleContentSize
    |> Option.iter (fun v -> view.VisibleContentSize <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ScrollSlider.OrientationChanged,
      view.OrientationChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ScrollSlider.OrientationChanging,
      view.OrientationChanging
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ScrollSlider.PositionChanged,
      view.PositionChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.ScrollSlider.PositionChanging,
      view.PositionChanging
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.ScrollSlider.Scrolled, view.Scrolled)


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> ScrollSlider

    // Properties
    props
    |> Props.tryFind PKey.ScrollSlider.Orientation
    |> Option.iter (fun _ -> view.Orientation <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ScrollSlider.Position
    |> Option.iter (fun _ -> view.Position <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ScrollSlider.Size
    |> Option.iter (fun _ -> view.Size <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ScrollSlider.SliderPadding
    |> Option.iter (fun _ -> view.SliderPadding <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.ScrollSlider.VisibleContentSize
    |> Option.iter (fun _ -> view.VisibleContentSize <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ScrollSlider.OrientationChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ScrollSlider.OrientationChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ScrollSlider.PositionChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ScrollSlider.PositionChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.ScrollSlider.Scrolled)

  let newView () : View = new ScrollSlider()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal SelectorBaseDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> SelectorBase

    // Properties
    props
    |> Props.tryFind PKey.SelectorBase.DoubleClickAccepts
    |> Option.iter (fun v -> view.DoubleClickAccepts <- v)

    props
    |> Props.tryFind PKey.SelectorBase.HorizontalSpace
    |> Option.iter (fun v -> view.HorizontalSpace <- v)

    props
    |> Props.tryFind PKey.SelectorBase.Labels
    |> Option.iter (fun v -> view.Labels <- v)

    props
    |> Props.tryFind PKey.SelectorBase.Orientation
    |> Option.iter (fun v -> view.Orientation <- v)

    props
    |> Props.tryFind PKey.SelectorBase.Styles
    |> Option.iter (fun v -> view.Styles <- v)

    props
    |> Props.tryFind PKey.SelectorBase.TabBehavior
    |> Option.iter (fun v -> view.TabBehavior <- v)

    props
    |> Props.tryFind PKey.SelectorBase.Value
    |> Option.iter (fun v -> view.Value <- v)

    props
    |> Props.tryFind PKey.SelectorBase.Values
    |> Option.iter (fun v -> view.Values <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.SelectorBase.OrientationChanged,
      view.OrientationChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.SelectorBase.OrientationChanging,
      view.OrientationChanging
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.SelectorBase.ValueChanged,
      view.ValueChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.SelectorBase.ValueChangedUntyped,
      view.ValueChangedUntyped
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.SelectorBase.ValueChanging,
      view.ValueChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> SelectorBase

    // Properties
    props
    |> Props.tryFind PKey.SelectorBase.DoubleClickAccepts
    |> Option.iter (fun _ -> view.DoubleClickAccepts <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.SelectorBase.HorizontalSpace
    |> Option.iter (fun _ -> view.HorizontalSpace <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.SelectorBase.Labels
    |> Option.iter (fun _ -> view.Labels <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.SelectorBase.Orientation
    |> Option.iter (fun _ -> view.Orientation <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.SelectorBase.Styles
    |> Option.iter (fun _ -> view.Styles <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.SelectorBase.TabBehavior
    |> Option.iter (fun _ -> view.TabBehavior <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.SelectorBase.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.SelectorBase.Values
    |> Option.iter (fun _ -> view.Values <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.SelectorBase.OrientationChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.SelectorBase.OrientationChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.SelectorBase.ValueChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.SelectorBase.ValueChangedUntyped)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.SelectorBase.ValueChanging)

  let newView () : View =
    failwith "Cannot instantiate abstract view type SelectorBase"

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal FlagSelectorDom =

  let applyProps (domNode: DomNode, props: Props) =
    SelectorBaseDom.applyProps (domNode, props)
    let view = domNode.View :?> FlagSelector

    // Properties
    props
    |> Props.tryFind PKey.FlagSelector.Value
    |> Option.iter (fun v -> view.Value <- v)


  let removeProps (domNode: DomNode, props: Props) =
    SelectorBaseDom.removeProps (domNode, props)
    let view = domNode.View :?> FlagSelector

    // Properties
    props
    |> Props.tryFind PKey.FlagSelector.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)


  let newView () : View = new FlagSelector()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = SelectorBaseDom.subElementsPropKeys


module internal OptionSelectorDom =

  let applyProps (domNode: DomNode, props: Props) =
    SelectorBaseDom.applyProps (domNode, props)
    let view = domNode.View :?> OptionSelector

    // Properties
    props
    |> Props.tryFind PKey.OptionSelector.FocusedItem
    |> Option.iter (fun v -> view.FocusedItem <- v)


  let removeProps (domNode: DomNode, props: Props) =
    SelectorBaseDom.removeProps (domNode, props)
    let view = domNode.View :?> OptionSelector

    // Properties
    props
    |> Props.tryFind PKey.OptionSelector.FocusedItem
    |> Option.iter (fun _ -> view.FocusedItem <- Unchecked.defaultof<_>)


  let newView () : View = new OptionSelector()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = SelectorBaseDom.subElementsPropKeys


module internal FlagSelector'Dom =

  let applyProps<'TFlagsEnum
    when 'TFlagsEnum: struct
    and 'TFlagsEnum: (new: unit -> 'TFlagsEnum)
    and 'TFlagsEnum :> System.Enum
    and 'TFlagsEnum :> System.ValueType>
    (domNode: DomNode, props: Props)
    =
    FlagSelectorDom.applyProps (domNode, props)
    let view = domNode.View :?> FlagSelector<'TFlagsEnum>

    // Properties
    props
    |> Props.tryFind PKey.FlagSelector'<'TFlagsEnum>.Value
    |> Option.iter (fun v -> view.Value <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.FlagSelector'<'TFlagsEnum>.ValueChanged,
      view.ValueChanged
    )


  let removeProps<'TFlagsEnum
    when 'TFlagsEnum: struct
    and 'TFlagsEnum: (new: unit -> 'TFlagsEnum)
    and 'TFlagsEnum :> System.Enum
    and 'TFlagsEnum :> System.ValueType>
    (domNode: DomNode, props: Props)
    =
    FlagSelectorDom.removeProps (domNode, props)
    let view = domNode.View :?> FlagSelector<'TFlagsEnum>

    // Properties
    props
    |> Props.tryFind PKey.FlagSelector'<'TFlagsEnum>.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.FlagSelector'<'TFlagsEnum>.ValueChanged)

  let newView<'TFlagsEnum
    when 'TFlagsEnum: struct
    and 'TFlagsEnum: (new: unit -> 'TFlagsEnum)
    and 'TFlagsEnum :> System.Enum
    and 'TFlagsEnum :> System.ValueType>
    ()
    : View =
    new FlagSelector<'TFlagsEnum>()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = FlagSelectorDom.subElementsPropKeys


module internal OptionSelector'Dom =

  let applyProps<'TEnum
    when 'TEnum: struct and 'TEnum: (new: unit -> 'TEnum) and 'TEnum :> System.Enum and 'TEnum :> System.ValueType>
    (domNode: DomNode, props: Props)
    =
    OptionSelectorDom.applyProps (domNode, props)
    let view = domNode.View :?> OptionSelector<'TEnum>

    // Properties
    props
    |> Props.tryFind PKey.OptionSelector'<'TEnum>.Value
    |> Option.iter (fun v -> view.Value <- v)

    props
    |> Props.tryFind PKey.OptionSelector'<'TEnum>.Values
    |> Option.iter (fun v -> view.Values <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.OptionSelector'<'TEnum>.ValueChanged,
      view.ValueChanged
    )


  let removeProps<'TEnum
    when 'TEnum: struct and 'TEnum: (new: unit -> 'TEnum) and 'TEnum :> System.Enum and 'TEnum :> System.ValueType>
    (domNode: DomNode, props: Props)
    =
    OptionSelectorDom.removeProps (domNode, props)
    let view = domNode.View :?> OptionSelector<'TEnum>

    // Properties
    props
    |> Props.tryFind PKey.OptionSelector'<'TEnum>.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.OptionSelector'<'TEnum>.Values
    |> Option.iter (fun _ -> view.Values <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.OptionSelector'<'TEnum>.ValueChanged)

  let newView<'TEnum
    when 'TEnum: struct and 'TEnum: (new: unit -> 'TEnum) and 'TEnum :> System.Enum and 'TEnum :> System.ValueType>
    ()
    : View =
    new OptionSelector<'TEnum>()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = OptionSelectorDom.subElementsPropKeys


module internal ShortcutDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> Shortcut

    // Properties
    props
    |> Props.tryFind PKey.Shortcut.Action
    |> Option.iter (fun v -> view.Action <- v)

    props
    |> Props.tryFind PKey.Shortcut.AlignmentModes
    |> Option.iter (fun v -> view.AlignmentModes <- v)

    props
    |> Props.tryFind PKey.Shortcut.BindKeyToApplication
    |> Option.iter (fun v -> view.BindKeyToApplication <- v)

    props
    |> Props.tryFind PKey.Shortcut.Command
    |> Option.iter (fun v -> view.Command <- v)

    props
    |> Props.tryFind PKey.Shortcut.CommandView
    |> Option.iter (fun v -> view.CommandView <- v)

    props
    |> Props.tryFind PKey.Shortcut.HelpText
    |> Option.iter (fun v -> view.HelpText <- v)

    props |> Props.tryFind PKey.Shortcut.Key |> Option.iter (fun v -> view.Key <- v)

    props
    |> Props.tryFind PKey.Shortcut.MinimumKeyTextSize
    |> Option.iter (fun v -> view.MinimumKeyTextSize <- v)

    props
    |> Props.tryFind PKey.Shortcut.Orientation
    |> Option.iter (fun v -> view.Orientation <- v)

    props
    |> Props.tryFind PKey.Shortcut.TargetView
    |> Option.iter (fun v -> view.TargetView <- v)

    props
    |> Props.tryFind PKey.Shortcut.Text
    |> Option.iter (fun v -> view.Text <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.Shortcut.OrientationChanged,
      view.OrientationChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.Shortcut.OrientationChanging,
      view.OrientationChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> Shortcut

    // Properties
    props
    |> Props.tryFind PKey.Shortcut.Action
    |> Option.iter (fun _ -> view.Action <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Shortcut.AlignmentModes
    |> Option.iter (fun _ -> view.AlignmentModes <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Shortcut.BindKeyToApplication
    |> Option.iter (fun _ -> view.BindKeyToApplication <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Shortcut.Command
    |> Option.iter (fun _ -> view.Command <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Shortcut.CommandView
    |> Option.iter (fun _ -> view.CommandView <- new View())

    props
    |> Props.tryFind PKey.Shortcut.HelpText
    |> Option.iter (fun _ -> view.HelpText <- "")

    props
    |> Props.tryFind PKey.Shortcut.Key
    |> Option.iter (fun _ -> view.Key <- Terminal.Gui.Input.Key.Empty)

    props
    |> Props.tryFind PKey.Shortcut.MinimumKeyTextSize
    |> Option.iter (fun _ -> view.MinimumKeyTextSize <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Shortcut.Orientation
    |> Option.iter (fun _ -> view.Orientation <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.Shortcut.TargetView
    |> Option.iter (fun _ -> view.TargetView <- new View())

    props
    |> Props.tryFind PKey.Shortcut.Text
    |> Option.iter (fun _ -> view.Text <- "")

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Shortcut.OrientationChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Shortcut.OrientationChanging)

  let newView () : View = new Shortcut()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list =
    ViewDom.subElementsPropKeys
    @ [ PKey.Shortcut.CommandView_element.key; PKey.Shortcut.TargetView_element.key ]


module internal MenuItemDom =

  let applyProps (domNode: DomNode, props: Props) =
    ShortcutDom.applyProps (domNode, props)
    let view = domNode.View :?> MenuItem

    // Properties
    props
    |> Props.tryFind PKey.MenuItem.SubMenu
    |> Option.iter (fun v -> view.SubMenu <- v)


  let removeProps (domNode: DomNode, props: Props) =
    ShortcutDom.removeProps (domNode, props)
    let view = domNode.View :?> MenuItem

    // Properties
    props
    |> Props.tryFind PKey.MenuItem.SubMenu
    |> Option.iter (fun _ -> view.SubMenu <- Unchecked.defaultof<_>)


  let newView () : View = new MenuItem()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list =
    ShortcutDom.subElementsPropKeys @ [ PKey.MenuItem.SubMenu_element.key ]


module internal MenuBarItemDom =

  let applyProps (domNode: DomNode, props: Props) =
    MenuItemDom.applyProps (domNode, props)
    let view = domNode.View :?> MenuBarItem

    // Properties
    props
    |> Props.tryFind PKey.MenuBarItem.PopoverMenu
    |> Option.iter (fun v -> view.PopoverMenu <- v)

    props
    |> Props.tryFind PKey.MenuBarItem.PopoverMenuOpen
    |> Option.iter (fun v -> view.PopoverMenuOpen <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.MenuBarItem.MenuOpenChanged,
      view.MenuOpenChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.MenuBarItem.PopoverMenuOpenChanged,
      view.PopoverMenuOpenChanged
    )


  let removeProps (domNode: DomNode, props: Props) =
    MenuItemDom.removeProps (domNode, props)
    let view = domNode.View :?> MenuBarItem

    // Properties
    props
    |> Props.tryFind PKey.MenuBarItem.PopoverMenu
    |> Option.iter (fun _ -> view.PopoverMenu <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.MenuBarItem.PopoverMenuOpen
    |> Option.iter (fun _ -> view.PopoverMenuOpen <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.MenuBarItem.MenuOpenChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.MenuBarItem.PopoverMenuOpenChanged)

  let newView () : View = new MenuBarItem()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list =
    MenuItemDom.subElementsPropKeys @ [ PKey.MenuBarItem.PopoverMenu_element.key ]


module internal SpinnerViewDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> SpinnerView

    // Properties
    props
    |> Props.tryFind PKey.SpinnerView.AutoSpin
    |> Option.iter (fun v -> view.AutoSpin <- v)

    props
    |> Props.tryFind PKey.SpinnerView.Sequence
    |> Option.iter (fun v -> view.Sequence <- v)

    props
    |> Props.tryFind PKey.SpinnerView.SpinBounce
    |> Option.iter (fun v -> view.SpinBounce <- v)

    props
    |> Props.tryFind PKey.SpinnerView.SpinDelay
    |> Option.iter (fun v -> view.SpinDelay <- v)

    props
    |> Props.tryFind PKey.SpinnerView.SpinReverse
    |> Option.iter (fun v -> view.SpinReverse <- v)

    props
    |> Props.tryFind PKey.SpinnerView.Style
    |> Option.iter (fun v -> view.Style <- v)


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> SpinnerView

    // Properties
    props
    |> Props.tryFind PKey.SpinnerView.AutoSpin
    |> Option.iter (fun _ -> view.AutoSpin <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.SpinnerView.Sequence
    |> Option.iter (fun _ -> view.Sequence <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.SpinnerView.SpinBounce
    |> Option.iter (fun _ -> view.SpinBounce <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.SpinnerView.SpinDelay
    |> Option.iter (fun _ -> view.SpinDelay <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.SpinnerView.SpinReverse
    |> Option.iter (fun _ -> view.SpinReverse <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.SpinnerView.Style
    |> Option.iter (fun _ -> view.Style <- Unchecked.defaultof<_>)


  let newView () : View = new SpinnerView()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal StatusBarDom =

  let applyProps (domNode: DomNode, props: Props) = BarDom.applyProps (domNode, props)

  let removeProps (domNode: DomNode, props: Props) = BarDom.removeProps (domNode, props)

  let newView () : View = new StatusBar()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = BarDom.subElementsPropKeys


module internal TabDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> Tab

    // Properties
    props
    |> Props.tryFind PKey.Tab.DisplayText
    |> Option.iter (fun v -> view.DisplayText <- v)

    props |> Props.tryFind PKey.Tab.View |> Option.iter (fun v -> view.View <- v)


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> Tab

    // Properties
    props
    |> Props.tryFind PKey.Tab.DisplayText
    |> Option.iter (fun _ -> view.DisplayText <- "")

    props
    |> Props.tryFind PKey.Tab.View
    |> Option.iter (fun _ -> view.View <- new View())


  let newView () : View = new Tab()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list =
    ViewDom.subElementsPropKeys @ [ PKey.Tab.View_element.key ]


module internal TabViewDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> TabView

    // Properties
    props
    |> Props.tryFind PKey.TabView.MaxTabTextWidth
    |> Option.iter (fun v -> view.MaxTabTextWidth <- v)

    props
    |> Props.tryFind PKey.TabView.SelectedTab
    |> Option.iter (fun v -> view.SelectedTab <- v)

    props
    |> Props.tryFind PKey.TabView.Style
    |> Option.iter (fun v -> view.Style <- v)

    props
    |> Props.tryFind PKey.TabView.TabScrollOffset
    |> Option.iter (fun v -> view.TabScrollOffset <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TabView.SelectedTabChanged,
      view.SelectedTabChanged
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.TabView.TabClicked, view.TabClicked)


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> TabView

    // Properties
    props
    |> Props.tryFind PKey.TabView.MaxTabTextWidth
    |> Option.iter (fun _ -> view.MaxTabTextWidth <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TabView.SelectedTab
    |> Option.iter (fun _ -> view.SelectedTab <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TabView.Style
    |> Option.iter (fun _ -> view.Style <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TabView.TabScrollOffset
    |> Option.iter (fun _ -> view.TabScrollOffset <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TabView.SelectedTabChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TabView.TabClicked)

  let newView () : View = new TabView()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list =
    ViewDom.subElementsPropKeys @ [ PKey.TabView.SelectedTab_element.key ]


module internal TableViewDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> TableView

    // Properties
    props
    |> Props.tryFind PKey.TableView.CellActivationKey
    |> Option.iter (fun v -> view.CellActivationKey <- v)

    props
    |> Props.tryFind PKey.TableView.CollectionNavigator
    |> Option.iter (fun v -> view.CollectionNavigator <- v)

    props
    |> Props.tryFind PKey.TableView.ColumnOffset
    |> Option.iter (fun v -> view.ColumnOffset <- v)

    props
    |> Props.tryFind PKey.TableView.FullRowSelect
    |> Option.iter (fun v -> view.FullRowSelect <- v)

    props
    |> Props.tryFind PKey.TableView.MaxCellWidth
    |> Option.iter (fun v -> view.MaxCellWidth <- v)

    props
    |> Props.tryFind PKey.TableView.MinCellWidth
    |> Option.iter (fun v -> view.MinCellWidth <- v)

    props
    |> Props.tryFind PKey.TableView.MultiSelect
    |> Option.iter (fun v -> view.MultiSelect <- v)

    props
    |> Props.tryFind PKey.TableView.NullSymbol
    |> Option.iter (fun v -> view.NullSymbol <- v)

    props
    |> Props.tryFind PKey.TableView.RowOffset
    |> Option.iter (fun v -> view.RowOffset <- v)

    props
    |> Props.tryFind PKey.TableView.SelectedColumn
    |> Option.iter (fun v -> view.SelectedColumn <- v)

    props
    |> Props.tryFind PKey.TableView.SelectedRow
    |> Option.iter (fun v -> view.SelectedRow <- v)

    props
    |> Props.tryFind PKey.TableView.SeparatorSymbol
    |> Option.iter (fun v -> view.SeparatorSymbol <- v)

    props
    |> Props.tryFind PKey.TableView.Style
    |> Option.iter (fun v -> view.Style <- v)

    props
    |> Props.tryFind PKey.TableView.Table
    |> Option.iter (fun v -> view.Table <- v)

    props
    |> Props.tryFind PKey.TableView.UseAllRowsForContentCalculation
    |> Option.iter (fun v -> view.UseAllRowsForContentCalculation <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TableView.CellActivated,
      view.CellActivated
    )

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.TableView.CellToggled, view.CellToggled)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TableView.SelectedCellChanged,
      view.SelectedCellChanged
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> TableView

    // Properties
    props
    |> Props.tryFind PKey.TableView.CellActivationKey
    |> Option.iter (fun _ -> view.CellActivationKey <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TableView.CollectionNavigator
    |> Option.iter (fun _ -> view.CollectionNavigator <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TableView.ColumnOffset
    |> Option.iter (fun _ -> view.ColumnOffset <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TableView.FullRowSelect
    |> Option.iter (fun _ -> view.FullRowSelect <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TableView.MaxCellWidth
    |> Option.iter (fun _ -> view.MaxCellWidth <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TableView.MinCellWidth
    |> Option.iter (fun _ -> view.MinCellWidth <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TableView.MultiSelect
    |> Option.iter (fun _ -> view.MultiSelect <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TableView.NullSymbol
    |> Option.iter (fun _ -> view.NullSymbol <- "")

    props
    |> Props.tryFind PKey.TableView.RowOffset
    |> Option.iter (fun _ -> view.RowOffset <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TableView.SelectedColumn
    |> Option.iter (fun _ -> view.SelectedColumn <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TableView.SelectedRow
    |> Option.iter (fun _ -> view.SelectedRow <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TableView.SeparatorSymbol
    |> Option.iter (fun _ -> view.SeparatorSymbol <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TableView.Style
    |> Option.iter (fun _ -> view.Style <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TableView.Table
    |> Option.iter (fun _ -> view.Table <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TableView.UseAllRowsForContentCalculation
    |> Option.iter (fun _ -> view.UseAllRowsForContentCalculation <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TableView.CellActivated)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TableView.CellToggled)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TableView.SelectedCellChanged)

  let newView () : View = new TableView()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal TextFieldDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> TextField

    // Properties
    props
    |> Props.tryFind PKey.TextField.Autocomplete
    |> Option.iter (fun v -> view.Autocomplete <- v)

    props
    |> Props.tryFind PKey.TextField.InsertionPoint
    |> Option.iter (fun v -> view.InsertionPoint <- v)

    props
    |> Props.tryFind PKey.TextField.ReadOnly
    |> Option.iter (fun v -> view.ReadOnly <- v)

    props
    |> Props.tryFind PKey.TextField.Secret
    |> Option.iter (fun v -> view.Secret <- v)

    props
    |> Props.tryFind PKey.TextField.SelectWordOnlyOnDoubleClick
    |> Option.iter (fun v -> view.SelectWordOnlyOnDoubleClick <- v)

    props
    |> Props.tryFind PKey.TextField.SelectedStart
    |> Option.iter (fun v -> view.SelectedStart <- v)

    props
    |> Props.tryFind PKey.TextField.Text
    |> Option.iter (fun v -> view.Text <- v)

    props
    |> Props.tryFind PKey.TextField.UseSameRuneTypeForWords
    |> Option.iter (fun v -> view.UseSameRuneTypeForWords <- v)

    props
    |> Props.tryFind PKey.TextField.Used
    |> Option.iter (fun v -> view.Used <- v)

    props
    |> Props.tryFind PKey.TextField.Value
    |> Option.iter (fun v -> view.Value <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TextField.TextChanging,
      view.TextChanging
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TextField.ValueChanged,
      view.ValueChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TextField.ValueChangedUntyped,
      view.ValueChangedUntyped
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TextField.ValueChanging,
      view.ValueChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> TextField

    // Properties
    props
    |> Props.tryFind PKey.TextField.Autocomplete
    |> Option.iter (fun _ -> view.Autocomplete <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextField.InsertionPoint
    |> Option.iter (fun _ -> view.InsertionPoint <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextField.ReadOnly
    |> Option.iter (fun _ -> view.ReadOnly <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextField.Secret
    |> Option.iter (fun _ -> view.Secret <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextField.SelectWordOnlyOnDoubleClick
    |> Option.iter (fun _ -> view.SelectWordOnlyOnDoubleClick <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextField.SelectedStart
    |> Option.iter (fun _ -> view.SelectedStart <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextField.Text
    |> Option.iter (fun _ -> view.Text <- "")

    props
    |> Props.tryFind PKey.TextField.UseSameRuneTypeForWords
    |> Option.iter (fun _ -> view.UseSameRuneTypeForWords <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextField.Used
    |> Option.iter (fun _ -> view.Used <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextField.Value
    |> Option.iter (fun _ -> view.Value <- "")

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TextField.TextChanging)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TextField.ValueChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TextField.ValueChangedUntyped)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TextField.ValueChanging)

  let newView () : View = new TextField()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal DropDownListDom =

  let applyProps (domNode: DomNode, props: Props) =
    TextFieldDom.applyProps (domNode, props)
    let view = domNode.View :?> DropDownList

    // Properties
    props
    |> Props.tryFind PKey.DropDownList.Source
    |> Option.iter (fun v -> view.Source <- v)


  let removeProps (domNode: DomNode, props: Props) =
    TextFieldDom.removeProps (domNode, props)
    let view = domNode.View :?> DropDownList

    // Properties
    props
    |> Props.tryFind PKey.DropDownList.Source
    |> Option.iter (fun _ -> view.Source <- Unchecked.defaultof<_>)


  let newView () : View = new DropDownList()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = TextFieldDom.subElementsPropKeys


module internal TextValidateFieldDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> TextValidateField

    // Properties
    props
    |> Props.tryFind PKey.TextValidateField.Provider
    |> Option.iter (fun v -> view.Provider <- v)

    props
    |> Props.tryFind PKey.TextValidateField.Text
    |> Option.iter (fun v -> view.Text <- v)

    props
    |> Props.tryFind PKey.TextValidateField.Value
    |> Option.iter (fun v -> view.Value <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TextValidateField.ValueChanged,
      view.ValueChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TextValidateField.ValueChangedUntyped,
      view.ValueChangedUntyped
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TextValidateField.ValueChanging,
      view.ValueChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> TextValidateField

    // Properties
    props
    |> Props.tryFind PKey.TextValidateField.Provider
    |> Option.iter (fun _ -> view.Provider <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextValidateField.Text
    |> Option.iter (fun _ -> view.Text <- "")

    props
    |> Props.tryFind PKey.TextValidateField.Value
    |> Option.iter (fun _ -> view.Value <- "")

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TextValidateField.ValueChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TextValidateField.ValueChangedUntyped)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TextValidateField.ValueChanging)

  let newView () : View = new TextValidateField()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal DateEditorDom =

  let applyProps (domNode: DomNode, props: Props) =
    TextValidateFieldDom.applyProps (domNode, props)
    let view = domNode.View :?> DateEditor

    // Properties
    props
    |> Props.tryFind PKey.DateEditor.Format
    |> Option.iter (fun v -> view.Format <- v)

    props
    |> Props.tryFind PKey.DateEditor.Value
    |> Option.iter (fun v -> view.Value <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.DateEditor.ValueChanged,
      view.ValueChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.DateEditor.ValueChangedUntyped,
      view.ValueChangedUntyped
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.DateEditor.ValueChanging,
      view.ValueChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    TextValidateFieldDom.removeProps (domNode, props)
    let view = domNode.View :?> DateEditor

    // Properties
    props
    |> Props.tryFind PKey.DateEditor.Format
    |> Option.iter (fun _ -> view.Format <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.DateEditor.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.DateEditor.ValueChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.DateEditor.ValueChangedUntyped)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.DateEditor.ValueChanging)

  let newView () : View = new DateEditor()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = TextValidateFieldDom.subElementsPropKeys


module internal TextViewDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> TextView

    // Properties
    props
    |> Props.tryFind PKey.TextView.EnterKeyAddsLine
    |> Option.iter (fun v -> view.EnterKeyAddsLine <- v)

    props
    |> Props.tryFind PKey.TextView.InheritsPreviousAttribute
    |> Option.iter (fun v -> view.InheritsPreviousAttribute <- v)

    props
    |> Props.tryFind PKey.TextView.InsertionPoint
    |> Option.iter (fun v -> view.InsertionPoint <- v)

    props
    |> Props.tryFind PKey.TextView.IsSelecting
    |> Option.iter (fun v -> view.IsSelecting <- v)

    props
    |> Props.tryFind PKey.TextView.Multiline
    |> Option.iter (fun v -> view.Multiline <- v)

    props
    |> Props.tryFind PKey.TextView.ReadOnly
    |> Option.iter (fun v -> view.ReadOnly <- v)

    props
    |> Props.tryFind PKey.TextView.ScrollBars
    |> Option.iter (fun v -> view.ScrollBars <- v)

    props
    |> Props.tryFind PKey.TextView.SelectWordOnlyOnDoubleClick
    |> Option.iter (fun v -> view.SelectWordOnlyOnDoubleClick <- v)

    props
    |> Props.tryFind PKey.TextView.SelectionStartColumn
    |> Option.iter (fun v -> view.SelectionStartColumn <- v)

    props
    |> Props.tryFind PKey.TextView.SelectionStartRow
    |> Option.iter (fun v -> view.SelectionStartRow <- v)

    props
    |> Props.tryFind PKey.TextView.TabKeyAddsTab
    |> Option.iter (fun v -> view.TabKeyAddsTab <- v)

    props
    |> Props.tryFind PKey.TextView.TabWidth
    |> Option.iter (fun v -> view.TabWidth <- v)

    props
    |> Props.tryFind PKey.TextView.Text
    |> Option.iter (fun v -> view.Text <- v)

    props
    |> Props.tryFind PKey.TextView.UseSameRuneTypeForWords
    |> Option.iter (fun v -> view.UseSameRuneTypeForWords <- v)

    props
    |> Props.tryFind PKey.TextView.Used
    |> Option.iter (fun v -> view.Used <- v)

    props
    |> Props.tryFind PKey.TextView.WordWrap
    |> Option.iter (fun v -> view.WordWrap <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TextView.ContentsChanged,
      view.ContentsChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TextView.DrawNormalColor,
      view.DrawNormalColor
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TextView.DrawReadOnlyColor,
      view.DrawReadOnlyColor
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TextView.DrawSelectionColor,
      view.DrawSelectionColor
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TextView.DrawUsedColor,
      view.DrawUsedColor
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TextView.UnwrappedCursorPositionChanged,
      view.UnwrappedCursorPositionChanged
    )


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> TextView

    // Properties
    props
    |> Props.tryFind PKey.TextView.EnterKeyAddsLine
    |> Option.iter (fun _ -> view.EnterKeyAddsLine <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextView.InheritsPreviousAttribute
    |> Option.iter (fun _ -> view.InheritsPreviousAttribute <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextView.InsertionPoint
    |> Option.iter (fun _ -> view.InsertionPoint <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextView.IsSelecting
    |> Option.iter (fun _ -> view.IsSelecting <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextView.Multiline
    |> Option.iter (fun _ -> view.Multiline <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextView.ReadOnly
    |> Option.iter (fun _ -> view.ReadOnly <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextView.ScrollBars
    |> Option.iter (fun _ -> view.ScrollBars <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextView.SelectWordOnlyOnDoubleClick
    |> Option.iter (fun _ -> view.SelectWordOnlyOnDoubleClick <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextView.SelectionStartColumn
    |> Option.iter (fun _ -> view.SelectionStartColumn <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextView.SelectionStartRow
    |> Option.iter (fun _ -> view.SelectionStartRow <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextView.TabKeyAddsTab
    |> Option.iter (fun _ -> view.TabKeyAddsTab <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextView.TabWidth
    |> Option.iter (fun _ -> view.TabWidth <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextView.Text
    |> Option.iter (fun _ -> view.Text <- "")

    props
    |> Props.tryFind PKey.TextView.UseSameRuneTypeForWords
    |> Option.iter (fun _ -> view.UseSameRuneTypeForWords <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextView.Used
    |> Option.iter (fun _ -> view.Used <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TextView.WordWrap
    |> Option.iter (fun _ -> view.WordWrap <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TextView.ContentsChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TextView.DrawNormalColor)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TextView.DrawReadOnlyColor)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TextView.DrawSelectionColor)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TextView.DrawUsedColor)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TextView.UnwrappedCursorPositionChanged)

  let newView () : View = new TextView()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal TimeEditorDom =

  let applyProps (domNode: DomNode, props: Props) =
    TextValidateFieldDom.applyProps (domNode, props)
    let view = domNode.View :?> TimeEditor

    // Properties
    props
    |> Props.tryFind PKey.TimeEditor.Format
    |> Option.iter (fun v -> view.Format <- v)

    props
    |> Props.tryFind PKey.TimeEditor.Value
    |> Option.iter (fun v -> view.Value <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TimeEditor.ValueChanged,
      view.ValueChanged
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TimeEditor.ValueChangedUntyped,
      view.ValueChangedUntyped
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TimeEditor.ValueChanging,
      view.ValueChanging
    )


  let removeProps (domNode: DomNode, props: Props) =
    TextValidateFieldDom.removeProps (domNode, props)
    let view = domNode.View :?> TimeEditor

    // Properties
    props
    |> Props.tryFind PKey.TimeEditor.Format
    |> Option.iter (fun _ -> view.Format <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TimeEditor.Value
    |> Option.iter (fun _ -> view.Value <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TimeEditor.ValueChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TimeEditor.ValueChangedUntyped)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TimeEditor.ValueChanging)

  let newView () : View = new TimeEditor()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = TextValidateFieldDom.subElementsPropKeys


module internal TreeViewDom =

  let applyProps<'T when 'T: not struct> (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> TreeView<'T>

    // Properties
    props
    |> Props.tryFind PKey.TreeView<'T>.AllowLetterBasedNavigation
    |> Option.iter (fun v -> view.AllowLetterBasedNavigation <- v)

    props
    |> Props.tryFind PKey.TreeView<'T>.AspectGetter
    |> Option.iter (fun v -> view.AspectGetter <- v)

    props
    |> Props.tryFind PKey.TreeView<'T>.ColorGetter
    |> Option.iter (fun v -> view.ColorGetter <- v)

    props
    |> Props.tryFind PKey.TreeView<'T>.MaxDepth
    |> Option.iter (fun v -> view.MaxDepth <- v)

    props
    |> Props.tryFind PKey.TreeView<'T>.MultiSelect
    |> Option.iter (fun v -> view.MultiSelect <- v)

    props
    |> Props.tryFind PKey.TreeView<'T>.ObjectActivationButton
    |> Option.iter (fun v -> view.ObjectActivationButton <- v)

    props
    |> Props.tryFind PKey.TreeView<'T>.ObjectActivationKey
    |> Option.iter (fun v -> view.ObjectActivationKey <- v)

    props
    |> Props.tryFind PKey.TreeView<'T>.ScrollOffsetHorizontal
    |> Option.iter (fun v -> view.ScrollOffsetHorizontal <- v)

    props
    |> Props.tryFind PKey.TreeView<'T>.ScrollOffsetVertical
    |> Option.iter (fun v -> view.ScrollOffsetVertical <- v)

    props
    |> Props.tryFind PKey.TreeView<'T>.SelectedObject
    |> Option.iter (fun v -> view.SelectedObject <- v)

    props
    |> Props.tryFind PKey.TreeView<'T>.Style
    |> Option.iter (fun v -> view.Style <- v)

    props
    |> Props.tryFind PKey.TreeView<'T>.TreeBuilder
    |> Option.iter (fun v -> view.TreeBuilder <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.TreeView<'T>.DrawLine, view.DrawLine)

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TreeView<'T>.ObjectActivated,
      view.ObjectActivated
    )

    EventHandlerHelpers.TrySetEventHandler(
      domNode.EventRegistrar,
      props,
      PKey.TreeView<'T>.SelectionChanged,
      view.SelectionChanged
    )


  let removeProps<'T when 'T: not struct> (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> TreeView<'T>

    // Properties
    props
    |> Props.tryFind PKey.TreeView<'T>.AllowLetterBasedNavigation
    |> Option.iter (fun _ -> view.AllowLetterBasedNavigation <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TreeView<'T>.AspectGetter
    |> Option.iter (fun _ -> view.AspectGetter <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TreeView<'T>.ColorGetter
    |> Option.iter (fun _ -> view.ColorGetter <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TreeView<'T>.MaxDepth
    |> Option.iter (fun _ -> view.MaxDepth <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TreeView<'T>.MultiSelect
    |> Option.iter (fun _ -> view.MultiSelect <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TreeView<'T>.ObjectActivationButton
    |> Option.iter (fun _ -> view.ObjectActivationButton <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TreeView<'T>.ObjectActivationKey
    |> Option.iter (fun _ -> view.ObjectActivationKey <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TreeView<'T>.ScrollOffsetHorizontal
    |> Option.iter (fun _ -> view.ScrollOffsetHorizontal <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TreeView<'T>.ScrollOffsetVertical
    |> Option.iter (fun _ -> view.ScrollOffsetVertical <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TreeView<'T>.SelectedObject
    |> Option.iter (fun _ -> view.SelectedObject <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TreeView<'T>.Style
    |> Option.iter (fun _ -> view.Style <- Unchecked.defaultof<_>)

    props
    |> Props.tryFind PKey.TreeView<'T>.TreeBuilder
    |> Option.iter (fun _ -> view.TreeBuilder <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TreeView<'T>.DrawLine)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TreeView<'T>.ObjectActivated)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.TreeView<'T>.SelectionChanged)

  let newView<'T when 'T: not struct> () : View = new TreeView<'T>()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys


module internal TreeView'Dom =

  let applyProps (domNode: DomNode, props: Props) =
    TreeViewDom.applyProps<Terminal.Gui.Views.ITreeNode> (domNode, props)

  let removeProps (domNode: DomNode, props: Props) =
    TreeViewDom.removeProps<Terminal.Gui.Views.ITreeNode> (domNode, props)

  let newView () : View = new TreeView()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = TreeViewDom.subElementsPropKeys


module internal WindowDom =

  let applyProps (domNode: DomNode, props: Props) = RunnableDom.applyProps (domNode, props)

  let removeProps (domNode: DomNode, props: Props) =
    RunnableDom.removeProps (domNode, props)

  let newView () : View = new Window()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = RunnableDom.subElementsPropKeys


module internal WizardDom =

  let applyProps (domNode: DomNode, props: Props) =
    Dialog'Dom.applyProps (domNode, props)
    let view = domNode.View :?> Wizard

    // Properties
    props
    |> Props.tryFind PKey.Wizard.CurrentStep
    |> Option.iter (fun v -> view.CurrentStep <- v)

    // Events
    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.Wizard.MovingBack, view.MovingBack)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.Wizard.MovingNext, view.MovingNext)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.Wizard.StepChanged, view.StepChanged)

    EventHandlerHelpers.TrySetEventHandler(domNode.EventRegistrar, props, PKey.Wizard.StepChanging, view.StepChanging)


  let removeProps (domNode: DomNode, props: Props) =
    Dialog'Dom.removeProps (domNode, props)
    let view = domNode.View :?> Wizard

    // Properties
    props
    |> Props.tryFind PKey.Wizard.CurrentStep
    |> Option.iter (fun _ -> view.CurrentStep <- Unchecked.defaultof<_>)

    // Events
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Wizard.MovingBack)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Wizard.MovingNext)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Wizard.StepChanged)
    EventHandlerHelpers.TryRemoveEventHandler(domNode.EventRegistrar, PKey.Wizard.StepChanging)

  let newView () : View = new Wizard()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list =
    Dialog'Dom.subElementsPropKeys @ [ PKey.Wizard.CurrentStep_element.key ]


module internal WizardStepDom =

  let applyProps (domNode: DomNode, props: Props) =
    ViewDom.applyProps (domNode, props)
    let view = domNode.View :?> WizardStep

    // Properties
    props
    |> Props.tryFind PKey.WizardStep.BackButtonText
    |> Option.iter (fun v -> view.BackButtonText <- v)

    props
    |> Props.tryFind PKey.WizardStep.HelpText
    |> Option.iter (fun v -> view.HelpText <- v)

    props
    |> Props.tryFind PKey.WizardStep.NextButtonText
    |> Option.iter (fun v -> view.NextButtonText <- v)


  let removeProps (domNode: DomNode, props: Props) =
    ViewDom.removeProps (domNode, props)
    let view = domNode.View :?> WizardStep

    // Properties
    props
    |> Props.tryFind PKey.WizardStep.BackButtonText
    |> Option.iter (fun _ -> view.BackButtonText <- "")

    props
    |> Props.tryFind PKey.WizardStep.HelpText
    |> Option.iter (fun _ -> view.HelpText <- "")

    props
    |> Props.tryFind PKey.WizardStep.NextButtonText
    |> Option.iter (fun _ -> view.NextButtonText <- "")


  let newView () : View = new WizardStep()

  let setAsChildOfParentView: bool = true

  let subElementsPropKeys: RawPropKey list = ViewDom.subElementsPropKeys
